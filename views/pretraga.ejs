<!DOCTYPE html>
<html lang="sr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pretraga - Lovački Savez Crne Gore</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include('partials/header') %>
    
    <div class="container">
        <div class="page-header">
            <h1>Pretraga lovačkih dozvola</h1>
        </div>

        <div class="search-container">
            <form method="POST" action="/pretraga" class="search-form">
                <div class="search-input-group">
                    <input 
                        type="text" 
                        name="search" 
                        placeholder="Pretražite po imenu, prezimenu, broju knjige..." 
                        value="<%= query %>"
                        autofocus
                    >
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-search"></i> Pretraži
                    </button>
                </div>
            </form>
        </div>

        <% if (lovci !== null) { %>
            <% if (lovci.length > 0) { %>
                <div class="search-results-info">
                    Pronađeno <strong><%= totalRecords %></strong> rezultata
                </div>
                <div class="table-container">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Br. knjige</th>
                                <th>Redni broj</th>
                                <th>Ime i prezime</th>
                                <th>Ime oca</th>
                                <th>Datum rođenja</th>
                                <th>Mjesto rođenja</th>
                                <th>Lovačka organizacija</th>
                                <th>Akcije</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% lovci.forEach(lovac => { %>
                                <tr>
                                    <td><%= lovac.broj_knjige ? lovac.broj_knjige : '/' %></td>
                                    <td><%= lovac.redni_broj ? lovac.redni_broj : '/' %></td>
                                    <td class="name-cell"><%= lovac.ime %> <%= lovac.prezime %></td>
                                    <td><%= lovac.ocevo_ime ? lovac.ocevo_ime : '/' %></td>
                                    <td><%= lovac.datum_rodjenja || '/' %></td>
                                    <td><%= lovac.mjesto_rodjenja ? lovac.mjesto_rodjenja : '/' %></td>
                                    <td><%= lovac.clan_lovacke_organizacije ? lovac.clan_lovacke_organizacije : '/' %></td>
                                    <td class="actions-cell">
                                        <a href="/dozvola/<%= lovac.id %>" class="btn-icon btn-view" title="Pregled">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a href="/uredi/<%= lovac.id %>" class="btn-icon btn-edit" title="Uredi">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>

                <% if (totalPages > 1) { %>
                    <div class="pagination">
                        <% if (currentPage > 1) { %>
                            <a href="/pretraga?search=<%= encodeURIComponent(query) %>&page=1" class="pagination-btn">
                                <i class="fas fa-angle-double-left"></i>
                            </a>
                            <a href="/pretraga?search=<%= encodeURIComponent(query) %>&page=<%= currentPage - 1 %>" class="pagination-btn">
                                <i class="fas fa-angle-left"></i>
                            </a>
                        <% } %>

                        <% 
                        let startPage = Math.max(1, currentPage - 2);
                        let endPage = Math.min(totalPages, currentPage + 2);
                        
                        if (startPage > 1) { %>
                            <a href="/pretraga?search=<%= encodeURIComponent(query) %>&page=1" class="pagination-btn">1</a>
                            <% if (startPage > 2) { %>
                                <span class="pagination-dots">...</span>
                            <% } %>
                        <% }
                        
                        for (let i = startPage; i <= endPage; i++) { %>
                            <a href="/pretraga?search=<%= encodeURIComponent(query) %>&page=<%= i %>" class="pagination-btn <%= i === currentPage ? 'active' : '' %>">
                                <%= i %>
                            </a>
                        <% }
                        
                        if (endPage < totalPages) { %>
                            <% if (endPage < totalPages - 1) { %>
                                <span class="pagination-dots">...</span>
                            <% } %>
                            <a href="/pretraga?search=<%= encodeURIComponent(query) %>&page=<%= totalPages %>" class="pagination-btn"><%= totalPages %></a>
                        <% } %>

                        <% if (currentPage < totalPages) { %>
                            <a href="/pretraga?search=<%= encodeURIComponent(query) %>&page=<%= currentPage + 1 %>" class="pagination-btn">
                                <i class="fas fa-angle-right"></i>
                            </a>
                            <a href="/pretraga?search=<%= encodeURIComponent(query) %>&page=<%= totalPages %>" class="pagination-btn">
                                <i class="fas fa-angle-double-right"></i>
                            </a>
                        <% } %>
                    </div>
                <% } %>
            <% } else { %>
                <div class="empty-state">
                    <div class="empty-icon"><i class="fas fa-search"></i></div>
                    <h2>Nema rezultata</h2>
                    <p>Pokušajte sa drugačijim pojmom pretrage.</p>
                </div>
            <% } %>
        <% } %>
    </div>
</body>
</html>